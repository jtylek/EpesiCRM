<h1 class="h4">Change site theme</h1>
<p>Selects the default theme used across the whole EPESI installation.</p>
{if $message}
<div class="alert alert-success">{$message}</div>
{/if}
<div class="epesi-admin-quickform">
	{$form_html}
</div>
